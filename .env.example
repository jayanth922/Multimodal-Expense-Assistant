# LLM + Embeddings (use AI Studio free tier)
GOOGLE_API_KEY=__REPLACE_ME__

# Model choices (defaults)
GENAI_MODEL=gemini-2.5-flash
EMBEDDING_PROVIDER=local      # local | gemini
EMBEDDING_MODEL=text-embedding-004

# Whether to use VertexAI (AI Studio) for Google GenAI calls; defaults to false for free tier
USE_VERTEXAI=False

# If using Google client libs that respect Google GenAI/Vertex flags, set this too
# 0 | 1, or false | true
GOOGLE_GENAI_USE_VERTEXAI=0

# RAG + DB
SQLITE_PATH=./db/expenses.sqlite
VECTOR_STORE=chroma           # chroma | faiss
